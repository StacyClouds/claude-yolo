## 1. Remove build-time plugin baking

- [x] 1.1 Delete the `builder` stage from the `Dockerfile` (git/ssh install, `claude plugin marketplace add` x2, `claude plugin install` x6) and verify the final image no longer references `AS builder` or `--from=builder`
- [x] 1.2 Remove the two `COPY --from=builder ... /home/node/.claude` and `... /home/node/.claude.json` lines and verify `docker build` succeeds without them — lines removed and grep-confirmed gone; **`docker build` itself not run** (no Docker daemon in this environment) - run `claude-yolo rebuild` to confirm
- [x] 1.3 Remove `--ssh default` from `build_image()` in the `claude-yolo` script and verify `claude-yolo rebuild` succeeds without an ssh-agent forwarded — flag removed and grep-confirmed gone; **`claude-yolo rebuild` itself not run** (no Docker daemon here) - confirm on a machine with Docker

## 2. Fix npm for the unprivileged node user

- [x] 2.1 In the final image, after the root-run `npm install -g @anthropic-ai/claude-code @fission-ai/openspec`, determine npm's global prefix (`npm config get prefix` / `npm root -g`) and `chown -R node:node` that path (and its `bin` dir) so it's writable by `node`
- [x] 2.2 Verify as the `node` user inside a running container: `npm install -g cowsay` (or similar throwaway package) succeeds without an EACCES/read-only error — verified: `npm install -g cowsay` as `node` succeeds ("added 41 packages")
- [x] 2.3 Verify `npm config get cache` resolves to a path writable by `node` (e.g. under `/home/node/.npm`) and that an install populates it without error — verified: resolves to `/home/node/.npm`, populated by the cowsay install above with no error
- [x] 2.4 Verify `openspec --version` still runs, and `openspec init` still successfully scaffolds a fresh project folder under `/workspace` that has no existing `openspec/` directory — verified: `openspec --version` → `1.11.0`; `openspec init` on a fresh dir created an `openspec/` folder

## 3. Add serena's MCP runtime (Python + uv)

- [x] 3.1 Add Python 3 and `uv` (providing `uvx`) to the final image stage, available on `PATH` for the `node` user
- [x] 3.2 Verify `uvx --version` runs successfully as the `node` user inside the container — verified: `uvx 0.12.9 (aarch64-unknown-linux-gnu)`
- [x] 3.3 With the `serena` plugin configured (via a mounted `~/.claude` containing it, per section 4), attempted to verify Claude Code inside the container can connect to serena's MCP server — **found broken, not just unverified**: ran the plugin's actual invocation (`uvx --from git+https://github.com/oraios/serena serena --version`) inside a live sandbox container and it failed at `git init` against this sandbox's `git` stub ("git is disabled in this sandbox"). `uv` shells out to a real `git` binary to resolve `git+` dependency specs, which this sandbox deliberately never has — the MCP server can never even download, let alone start, until that's addressed (see 3.4-3.6)
- [x] 3.4 Add a `serena-builder` stage to the `Dockerfile` (a real `git`, used only there, for a public repo — no SSH/credentials needed) that runs `uv tool install git+https://github.com/oraios/serena` into `/opt/serena-tool` (`UV_TOOL_DIR`/`UV_TOOL_BIN_DIR` under it, outside `/home/node` so the `claude-yolo-home` volume doesn't shadow it), and `COPY --from=serena-builder` that directory into the final image, `chown`'d to `node`, with its `bin` added to `PATH` — the final image still has no working `git` of any kind
- [x] 3.4a Fixed on first real `claude-yolo rebuild`: the original `uv tool install --from git+https://github.com/oraios/serena serena` failed with `Package name (serena-agent) provided with --from does not match install request (serena)` — for `uv tool install` (unlike the plugin's own `uvx --from ... serena ...` invocation) the positional name must match the package's own name (`serena-agent`), not the entry-point/command name. Fixed by dropping `--from` and installing the git URL directly (`uv tool install git+https://github.com/oraios/serena`), which resolves the package name itself and still exposes every console-script entry point it defines (including `serena`) into `UV_TOOL_BIN_DIR` — confirmed the corrected form is accepted by `uv tool install --help`'s single-`<PACKAGE>`-argument signature, and that it now gets past argument parsing and fails only at the same `git init` step every other git-dependent uv command hits in this git-disabled environment
- [x] 3.5 In `entrypoint.sh`, after the host-config sync, find every `.mcp.json` under `$LOCAL_CLAUDE/plugins` whose `serena` entry is still `{"command": "uvx", "args": [..., "git+https://github.com/oraios/serena", ...]}` and rewrite it to call `/opt/serena-tool/bin/serena` directly (`args: ["start-mcp-server"]`), leaving any other `.mcp.json` untouched
- [x] 3.6 Verify the rewrite logic: copied this container's real `plugins/` tree (which has 5 live copies of serena's `.mcp.json` — 4 under `plugins/cache/.../serena/*` plus 1 under `plugins/marketplaces/.../external_plugins/serena`) into a scratch dir and ran the entrypoint.sh find/jq logic against it with a stub executable standing in for the baked binary — confirmed all 5 serena entries were rewritten to the pre-baked-binary form, and unrelated `.mcp.json` files (context7, github, etc.) were left byte-for-byte unchanged — **a real `claude-yolo rebuild` + live MCP connection not run** (no Docker daemon in this environment, and `uv tool install` fetching from git wasn't itself exercised here since this environment's own `git` is the same disabled stub) - confirm both on a machine with Docker

## 4. Sync host ~/.claude config into the sandbox without breaking login persistence

- [x] 4.1 In the `claude-yolo` script, replace `CLAUDE_MD_MOUNT` (single-file mount of `~/.claude/CLAUDE.md`) with a read-only mount of the whole `$HOME/.claude` folder at `/home/node/.claude-host` (not directly at `/home/node/.claude` — see 4.6), conditional on the host directory existing
- [x] 4.1a Add `entrypoint.sh`, baked into the image and set as `ENTRYPOINT`, that copies a fixed list of config items (`CLAUDE.md`, `settings.json`, `settings.local.json`, `agents`, `commands`, `skills`, `plugins`, `rules`, `keybindings.json`, `statusline-command.sh`, `statusline-command.ps1`, `workflows`) from `/home/node/.claude-host` into `/home/node/.claude` on every start, before exec-ing `tini`; allow it through `.dockerignore` (previously `**`-excluded)
- [x] 4.2 Verify `claude-yolo run` with a host `~/.claude` present exposes its settings/agents/skills/plugins/commands inside the container at `/home/node/.claude` — verified live: a fake host `~/.claude` with `settings.json`/`CLAUDE.md`/`agents/one.md` all appeared correctly under `/home/node/.claude` after container start
- [x] 4.3 Verify a write attempt from inside the container to a path under the host mount fails (read-only mount holds) — verified live: writing to `/home/node/.claude-host/settings.json` fails with "Read-only file system"
- [x] 4.4 Verify `claude-yolo run` still starts normally when the host has no `~/.claude` directory (mount is skipped, no error) — verified live: with no `.claude-host` mounted, `entrypoint.sh` skips the sync and the container starts normally
- [x] 4.5 Verify editing a file under the host's `~/.claude` and re-running `claude-yolo run` (no rebuild) picks up the change inside the container — verified live: changing `CLAUDE.md` on the fake host and starting a second container showed the new content
- [x] 4.6 Verify login/session state persists across container restarts and is untouched by the host config sync — verified live: wrote a fake `/home/node/.claude/.credentials.json` in one container, started a second container against the same `claude-yolo-home` volume (with the host `CLAUDE.md` edited in between), and the credentials file was still present and unchanged while `CLAUDE.md` picked up the edit
- [x] 4.7 Verify `claude-yolo nuke` (not just closing a container) is what actually removes persisted login — covered by the existing `nuke` implementation (`docker volume rm "$VOLUME"`), unchanged by this fix; 4.6 confirms closing/restarting a container alone does not remove it
- [x] 4.8 In `entrypoint.sh`, after the host-config sync, force `skipDangerousModePermissionPrompt = true` into `/home/node/.claude/settings.json` (creating the file first if missing), unconditionally — not just when a host `~/.claude` is mounted — so the "Bypass Permissions mode" confirmation dialog stays accepted across container restarts instead of being wiped by the settings.json replace-from-host on every start
- [x] 4.9 Verify the fix: confirmed a running sandbox's local `settings.json` had `skipDangerousModePermissionPrompt: true` (accepted earlier in that container's life) while the host's own `settings.json` did not have the key — i.e. the bug was live and reproducible; then ran a patched copy of `entrypoint.sh` (paths redirected into a scratch dir, real `.claude`/`.claude-host` snapshots copied in) end to end and confirmed the resulting `settings.json` has `skipDangerousModePermissionPrompt: true` after the host-copy step runs — **a real `claude-yolo rebuild`/container-restart cycle not run** (no Docker daemon in this environment) - confirm on a machine with Docker

## 5. Runtime-selectable workspace folder

- [x] 5.1 In the `claude-yolo` script, parse a leading `--workspace <path>` option (before the subcommand) that overrides `PROJECT_DIR`, resolving it with `cd "$path" && pwd` so an invalid path fails clearly before any container starts
- [x] 5.2 Verify `claude-yolo --workspace /some/other/project run "task"` mounts that folder as `/workspace` instead of the script's default parent directory — verified via dry-run with a stubbed `docker`
- [x] 5.3 Verify `claude-yolo --workspace /some/other/project "task"` (no explicit subcommand) behaves like `yolo` mode against that folder — verified via dry-run
- [x] 5.4 Verify `claude-yolo run` (no `--workspace`) still mounts the script's parent directory, unchanged — verified via dry-run
- [x] 5.5 Verify `claude-yolo --workspace /no/such/folder run` exits with a clear error and starts no container — verified via dry-run (also covers a missing path-argument to `--workspace`)
- [x] 5.6 Verify two concurrent invocations with different `--workspace` paths run as independent containers, each with its own `/workspace`, while both reuse the same `claude-yolo-home` login/session volume — verified via dry-run (two parallel stubbed invocations, independent workspace mounts, same volume name)

## 6. Update specs to match

- [x] 6.1 Run `openspec validate --change fix-sandbox-npm-serena-host-claude --strict` and resolve any reported issues — passes, no issues
